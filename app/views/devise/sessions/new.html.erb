<div class='devise-container devise-sign-in'>
  <h2>Inicia Sesión</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :email, 'Correo' %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'Ingresa tu correo' %>
    </div>

    <div class="field">
      <%= f.label :password, t('devise.label.password') %>
      <%= f.password_field :password, autocomplete: "current-password", placeholder: 'Ingresa tu constraseña' %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, 'Recordar sesión' %>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit "Iniciar Sesión" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
